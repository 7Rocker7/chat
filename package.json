{
  "name": "chat",
  "version": "1.0.0",
  "description": "Real-time multimedia chat application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  },
  "keywords": ["chat", "realtime", "socket.io", "react"],
  "author": "",
  "license": "MIT",
  "overrides": {
    "tar": "^7.4.3",
    "@tootallnate/once": "^2.0.1"
  }
}
